+++
title = "[C#] null과 is, as"
date = 2018-07-29T00:00:00+00:00
[taxonomies]
tags = ["C＃"]
[extra]
naver_log_no = 221328893656
naver_category_no = 25
+++

nullable  
null은 아무것도 가리키지 않음을 의미하는 일종의 상수 포인터다.  
그래서 레퍼런스 변수에는 사용에 아무런 문제가 없다.  

근데 값 타입을 다룰 때 문제가 발생한다.  
int 변수를 보자. 사용자에게서 정수값을 받아서 처리를 해야하는 상황이고, 사용자에게서 잘못된 값을 받았거나, 받았지 않았을 경우를 어떻게 표현해야 할까? 0? 0 자체도 값인 상황이 있을 수 있다.  
C는 null을 그냥 0으로 표현해버리지마는.. C#은 이 부분에서는 엄격하게 처리를 한다.  
그러면 값 타입에서 아무것도 없다는걸 어떻게 나타내야 할까  
이럴 때 값 타입에 null을 표현하기 위해 생긴게 nullable이다.  

원래 제네릭방식으로  
Nullable<int> num;  
이러면 null을 담을 수 있는 int 타입이 완성된다. 근데 썩 보기 좋지는 않다.  

여기에서 설탕을 치면  
int? num;  
이렇게 표현된다.  

근데 헷갈릴만한게 좀 있으니 문서를 참고해보자  
[https://docs.microsoft.com/ko-kr/dotnet/csharp/programming-guide/nullable-types/using-nullable-types](https://docs.microsoft.com/ko-kr/dotnet/csharp/programming-guide/nullable-types/using-nullable-types)  

![image](/images/img_b276a044437fa0ff.)
[Nullable 형식 사용(C# 프로그래밍 가이드)Nullable 형식 사용(C# 프로그래밍 가이드) 읽는 데 8분 참가자 nullable 형식은 기본 형식의 모든 값과 추가 null 값을 나타낼 수 있습니다. nullable 형식은 다음과 같은 두 가지 방법 중 하나로 선언됩니다. System.Nullable<T> variable 또는 T? variable T 는 nullable 형식의 기본 형식입니다. T 는 struct 를 비롯한 모든 값 형식일 수 있으나, 참조 형식일 수는 없습니다. nullable 형식을 사용할 수 있는 경우의 예를 들기 위해 일반 부울 변수가 두 가지 ...docs.microsoft.com](https://docs.microsoft.com/ko-kr/dotnet/csharp/programming-guide/nullable-types/using-nullable-types)
![](/images/img_9f91a3123635ee77.gif)
[https://msdn.microsoft.com/ko-kr/library/b3h38hb0(v=vs.110).aspx](https://msdn.microsoft.com/ko-kr/library/b3h38hb0(v=vs.110).aspx)[Nullable(T) 구조체 (System)이 문서는 기계 번역을 이용하여 번역되었습니다. 문서를 영문으로 보려면 영문 확인란을 선택하세요. 마우스 포인터를 텍스트 위로 이동시켜 팝업 창에서 영문 텍스트를 표시할 수도 있습니다. 번역 영문 Nullable <T> 구조체 .NET Framework (current version) 다른 버전 Visual Studio 2010 .NET Framework 4 Silverlight Visual Studio 2008 .NET Framework 3.5 .NET Framework 3.0 .NET Framework 2.0 게시 날짜: 201...msdn.microsoft.com](https://msdn.microsoft.com/ko-kr/library/b3h38hb0(v=vs.110).aspx)
![](/images/img_9f91a3123635ee77.gif)

<br>

As  
as는 캐스팅 연산자다. 이건 그냥 소괄호 캐스팅과는 다르게 참조타입으로의 변환만 허용을 한다.  
그리고 실패하면 에러가 아니라 null을 반환한다.  
이 null의 반환가능성 때문에 참조타입으로만 변환이 되는 것이다.  
용법은 대강 이렇다.  

변환할값 as 변환할타입  

<br>

<br>

<br>

Is  
is는 원래 그냥 타입변환 가능여부만 체크하는 연산자였다.  

변수 is 타입;  

해서 변환이 되는 상황이면 true를 반환하고 끝이었는데,  

버전 7.0부터 as의 기능을 어느정도 포함하게 되었다.  

변수 is 타입 바인딩변수;  
해서 is의 결과가 true면 저기다 바인딩을 해준다.  

if의 조건문 내에도 사용이 가능하다. 이거도 패턴매칭이라고 부르더라
